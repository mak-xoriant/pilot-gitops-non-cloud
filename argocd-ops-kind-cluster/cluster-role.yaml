apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRole
metadata:
  name: argocd-application-controller
rules:
  - apiGroups: ["*"]  # "*" allows all API groups
    resources: ["*"]  # "*" allows all resource types
    verbs: ["*"] 